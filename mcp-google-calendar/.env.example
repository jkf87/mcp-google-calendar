# Firebase 서비스 계정 파일 경로
# Firebase 콘솔에서 다운로드한 서비스 계정 JSON 파일 경로를 지정하세요
FIREBASE_SERVICE_ACCOUNT_PATH=./path-to-your-firebase-credentials.json

# 리디렉션 URI (선택 사항)
# Google OAuth2 인증 후 리디렉션될 URI를 지정하세요
# 기본값: http://localhost:3000/oauth2callback
REDIRECT_URI=http://localhost:3000/oauth2callback

# 타임존 (선택 사항)
# 기본값: Asia/Seoul
TIMEZONE=Asia/Seoul 